import { defineComponent } from "../../DOM";

const mounted = { current: false };

const render = () => {};
const destroy = () => {};

export default defineComponent({
  mounted,
  render,
  destroy
});
